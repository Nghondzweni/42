#include "libft.h"

void	*ft_memccpy(void *dest, const void *src, int c, size_t n)
{
	char	*s;
	char	*d;
	size_t	i;

	d = (char *)dest;
	while(n)
	{
		if(s[i] == c)
		{
			
		}
		d[i] = s[i];
		n--;
	}
}
